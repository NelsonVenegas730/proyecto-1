<div>
    <main class="error-container">
        <h1 class="error-code">?</h1>
        <h2 class="error-title"><%= title %></h2>
        <p class="error-description"><%= typeof description !== 'undefined' ? description : '' %></p>
        <% if (helpText || tooltip) { %>
            <p class="error-help">
                <%= helpText || '' %>
                <a class="go-back-link" href="<%= href || '#' %>"><%= tooltip || '' %></a>
            </p>
        <% } %>
    </main>
</div>